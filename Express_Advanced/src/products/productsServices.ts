import type { Request, Response, NextFunction } from "express";


export const getAllProducts = async() => {
    console.log("get all products");
    // if (true) {
    //     throw new Error("no product find yet");
    // }
    return [];
}